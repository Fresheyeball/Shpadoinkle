module Shpadoinkle.Streaming
  ( consumeStream
  ) where


{-
 - import Shpadoinkle

import Data.Functor.Of (Of)
import Streaming (Stream)
import Streaming.Internal (destroy)


consumeStream :: Stream (Of a) m () -> (a -> 
consumeStream = consumeStream
-}

consumeStream :: a
consumeStream = consumeStream
